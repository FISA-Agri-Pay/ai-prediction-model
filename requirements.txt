# Target: Windows local development with Python 3.13.
# Use Python 3.13-compatible wheels to avoid local source builds on Windows.
# Torch uses the default PyPI wheel; use the PyTorch index URL separately when CUDA/ROCm is required.
numpy>=2.1,<2.3
pandas>=2.2,<2.3
matplotlib>=3.10,<3.11
scikit-learn>=1.6,<1.7
prophet>=1.3,<1.4
statsmodels>=0.14,<0.15
torch>=2.6,<3.0
optuna>=4.0,<5.0
